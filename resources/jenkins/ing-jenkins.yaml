apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: jenkins-ingress
  namespace: jenkins
spec:
  rules:
  - host: jenkins.domain.placeholder
    http:
      paths:
      - backend:
          serviceName: jenkins
          servicePort: 8080
        path: /
tls: